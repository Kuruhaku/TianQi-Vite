TODO: General
> Adjust the svg add clouds, adjust the tree and give more texture in sun and moon.
> Fix the UI in weather page in detail area.

TODO: Current-detail-container
> Add card that has the current date.
> Add current-details-container Feels like: 26.7, wind speed, air pressure.
> Added rain particle movement if malakas yung rain.
> Add Foggy Area

TODO: In Weather API:
> There an input that will listen to the user input and get what data sys.is need to display.
> Display must be hiden and it will only show when the user only has already give a value.
> It must display the country, area, temperature, humidity, weather condition and current date. (more to add).
> It must catch an error if there country same as the user input.
> Make the information stay even has been move from other site.

IMPORTANT: Necessary Package in order to make this application working:
> npm install luxon (for time functionality)
> npm install express (for the backend)
> npm install dotenv (for .env)
> npm install cors (for cross origin)